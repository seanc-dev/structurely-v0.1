<% include partials/header %>
<div class="container">
    <section class="align-items-center">
        <div class="row text-center d-flex pt-4 pb-3 mb-0">
            <div class="col-12">
                <h3 class="display-1">Structurely</h3>
                <p class="lead">A Simple Tool for Structured Writing</p>
                <p>
                    <% if(!currentUser){ %>
                        <a href="/login" class="btn btn-secondary-outline">Log In</a>
                        <a href="/signup" class="btn btn-secondary-info">Sign Up</a>
                    <% } else { %>
                        <a href="/articles" class="btn btn-secondary-info">Your Articles</a>
                        <a href="/logout" class="btn btn-secondary-outline">Log Out</a>
                    <% } %>
                </p>
            </div>
        </div>
    </section>
</div>


<% include partials/footer %>